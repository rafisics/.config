# Implementation Plan: Document --lit flag in CLAUDE.md and command reference

- **Task**: 691 - Document --lit flag in CLAUDE.md and command reference
- **Status**: [COMPLETED]
- **Effort**: 1 hour
- **Dependencies**: Task 689 (literature-retrieve.sh), Task 690 (command-layer --lit wiring)
- **Research Inputs**: specs/691_document_lit_flag_claude_md/reports/01_document-lit-flag.md
- **Artifacts**: plans/01_document-lit-flag-plan.md (this file)
- **Standards**: plan-format.md, status-markers.md, artifact-management.md, tasks.md
- **Type**: meta
- **Lean Intent**: false

## Overview

Add `--lit` flag documentation to the CLAUDE.md merge source and the memory extension EXTENSION.md. Three insertion points: (1) Command Reference table rows for /research, /plan, /implement, /orchestrate; (2) a new "Literature Mode (`--lit`)" section after the existing Hard Mode section; (3) a "Literature-Augmented Research" subsection in the memory extension EXTENSION.md. Because CLAUDE.md is auto-generated by `merge.lua` only on extension load/unload cycles, the same edits must be applied directly to `.claude/CLAUDE.md` for immediate effect.

### Research Integration

Research report (01_document-lit-flag.md) identified exact line numbers for all three insertion points, confirmed the merge-source architecture (claudemd.md is the canonical source, EXTENSION.md files are read in-place), and pre-drafted the Literature Mode section content including the `--clean`/`--lit` interaction table and composability notes.

### Prior Plan Reference

No prior plan.

### Roadmap Alignment

No ROADMAP.md items are directly addressed by this documentation task.

## Goals & Non-Goals

**Goals**:
- Command Reference table shows `[--lit]` in usage strings for /research, /plan, /implement, /orchestrate
- New "Literature Mode (`--lit`)" section explains the flag's behavior, `specs/literature/` directory convention, flag interactions, and composability
- Memory EXTENSION.md documents `--lit` alongside `--clean` with a "Literature-Augmented Research" subsection
- Both the merge source (permanent) and generated CLAUDE.md (immediate) reflect the changes

**Non-Goals**:
- Adding `--hard`, `--team`, or model flags to the `/orchestrate` row (pre-existing omission, out of scope)
- Creating or populating `specs/literature/` directory
- Modifying any scripts or command files (already done in tasks 689-690)
- Updating the Multi-task syntax note to mention `--lit` (the current general wording already covers all flags)

## Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| CLAUDE.md gets overwritten on next extension cycle before user sees changes | L | M | Edit both merge source (permanent) and CLAUDE.md (immediate); next regeneration uses updated merge source |
| Inconsistency between merge source and generated CLAUDE.md if edits diverge | M | L | Phase 2 includes grep-based consistency verification across both files |
| Orchestrate row showing only `[--lit]` while omitting other flags looks incomplete | L | L | Out of scope; document as non-goal |

## Implementation Phases

**Dependency Analysis**:
| Wave | Phases | Blocked by |
|------|--------|------------|
| 1 | 1 | -- |
| 2 | 2 | 1 |

Phases within the same wave can execute in parallel.

### Phase 1: Edit source files (merge source + EXTENSION.md) [COMPLETED]

**Goal**: Add `--lit` documentation to the two canonical source files that feed into CLAUDE.md generation.

**Tasks**:
- [x] Update Command Reference table in `.claude/extensions/core/merge-sources/claudemd.md`:
  - `/research` row: add `[--lit]` after `[--clean]`
  - `/plan` row: add `[--lit]` after `[--clean]`
  - `/implement` row: add `[--lit]` after `[--clean]`
  - `/orchestrate` row: change `/orchestrate N` to `/orchestrate N [--lit]`
  *(completed)*
- [x] Insert new "## Literature Mode (`--lit`)" section in `.claude/extensions/core/merge-sources/claudemd.md` after the Hard Mode "Per-Invocation Only" paragraph (after line 285) and before "## Rules References" (line 287). Section content covers:
  - What `--lit` does (literature-retrieve.sh, TOKEN_BUDGET=4000, MAX_FILES=10, `<literature-context>` block)
  - `specs/literature/` directory convention (user-maintained, .md/.txt files, not task-scoped)
  - When to use `--lit` (paper-to-code, spec-to-implementation, H3 reference grounding)
  - Relationship to `--clean` (independence table showing 4 flag combinations)
  - Composability with `--team`, `--hard`, `--fast`, model flags
  - Per-invocation only (no sticky state)
  *(completed)*
- [x] Add "### Literature-Augmented Research" subsection in `.claude/extensions/memory/EXTENSION.md` after the "Memory-Augmented Research" subsection (after line 29, before "### Memory Lifecycle") *(completed)*

**Timing**: 30 minutes

**Depends on**: none

**Files to modify**:
- `.claude/extensions/core/merge-sources/claudemd.md` - Command Reference table rows (lines 90-92, 102) + new Literature Mode section (insert after line 285)
- `.claude/extensions/memory/EXTENSION.md` - New Literature-Augmented Research subsection (insert after line 29)

**Verification**:
- `grep -c '\-\-lit' .claude/extensions/core/merge-sources/claudemd.md` returns at least 10 (4 table rows + section content)
- `grep -c '\-\-lit' .claude/extensions/memory/EXTENSION.md` returns at least 2
- `grep '## Literature Mode' .claude/extensions/core/merge-sources/claudemd.md` finds the new section heading

---

### Phase 2: Mirror changes to generated CLAUDE.md and verify consistency [COMPLETED]

**Goal**: Apply identical edits to the generated `.claude/CLAUDE.md` for immediate visibility, then verify consistency between all three files.

**Tasks**:
- [x] Update Command Reference table in `.claude/CLAUDE.md`:
  - Same 4 row changes as Phase 1 (add `[--lit]` to /research, /plan, /implement, /orchestrate)
  *(completed)*
- [x] Insert the same "## Literature Mode (`--lit`)" section in `.claude/CLAUDE.md` after the Hard Mode "Per-Invocation Only" paragraph and before "## Rules References" *(completed)*
- [x] Insert the same "### Literature-Augmented Research" subsection in `.claude/CLAUDE.md` after "### Memory-Augmented Research" and before "### Memory Lifecycle" *(completed)*
- [x] Run consistency verification:
  - `grep '\-\-lit' .claude/extensions/core/merge-sources/claudemd.md` and `grep '\-\-lit' .claude/CLAUDE.md` should show matching content
  - `grep 'Literature-Augmented' .claude/extensions/memory/EXTENSION.md` and `grep 'Literature-Augmented' .claude/CLAUDE.md` should both return hits
  - `grep '## Literature Mode' .claude/CLAUDE.md` confirms the section exists in the generated file

**Timing**: 30 minutes

**Depends on**: 1

**Files to modify**:
- `.claude/CLAUDE.md` - Command Reference table rows + Literature Mode section + Literature-Augmented Research subsection (mirroring Phase 1 edits)

**Verification**:
- `grep -c '\-\-lit' .claude/CLAUDE.md` returns at least 12 (4 table rows + section content + memory subsection)
- All three `--lit` documentation locations are present and consistent:
  1. Command Reference table in merge source and CLAUDE.md both show `[--lit]` in 4 command rows
  2. Literature Mode section appears in both merge source and CLAUDE.md
  3. Literature-Augmented Research subsection appears in both EXTENSION.md and CLAUDE.md

---

## Testing & Validation

- [ ] `grep '\[--lit\]' .claude/extensions/core/merge-sources/claudemd.md` shows 4 matches (one per command row)
- [ ] `grep '## Literature Mode' .claude/extensions/core/merge-sources/claudemd.md` returns 1 match
- [ ] `grep 'Literature-Augmented Research' .claude/extensions/memory/EXTENSION.md` returns 1 match
- [ ] `grep '\[--lit\]' .claude/CLAUDE.md` shows 4 matches (mirrored command rows)
- [ ] `grep '## Literature Mode' .claude/CLAUDE.md` returns 1 match
- [ ] `grep 'Literature-Augmented Research' .claude/CLAUDE.md` returns 1 match
- [ ] No broken markdown tables (spot-check table alignment in all edited files)

## Artifacts & Outputs

- `specs/691_document_lit_flag_claude_md/plans/01_document-lit-flag-plan.md` (this plan)
- `.claude/extensions/core/merge-sources/claudemd.md` (updated merge source)
- `.claude/extensions/memory/EXTENSION.md` (updated memory extension)
- `.claude/CLAUDE.md` (mirrored changes for immediate effect)

## Rollback/Contingency

All changes are additive text insertions. To revert:
1. Remove `[--lit]` from the 4 Command Reference table rows in both merge source and CLAUDE.md
2. Delete the "## Literature Mode (`--lit`)" section from both merge source and CLAUDE.md
3. Delete the "### Literature-Augmented Research" subsection from both EXTENSION.md and CLAUDE.md
4. Alternatively, `git checkout` the three files to their pre-edit state
